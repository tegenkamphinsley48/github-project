// Generate a random Rust code for your project on GitHub
fn main() {
    let my_vec = vec![1, 2, 3];
    let mut sum = 0;
    for i in my_vec {
        sum += i;
    }
    println!("Sum: {}", sum);
}
